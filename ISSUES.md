# AutoScreen Customer Portal — ISSUES.md

> **Portal:** `autoscreen_client` (React + Vite · Port 7002)
> **Reference:** See `CLAUDE.md` for full architecture & workflow docs.

---

## 📋 Status Legend

| Badge            | Meaning                                        |
| ---------------- | ---------------------------------------------- |
| 🔴 `OPEN`        | Confirmed bug / missing feature, not yet fixed |
| 🟡 `IN PROGRESS` | Actively being worked on                       |
| 🟢 `FIXED`       | Resolved and verified                          |
| 🔵 `PLANNED`     | Scheduled for a future sprint                  |
| ⚪ `WONTFIX`     | Acknowledged, will not be addressed            |

---

## 📋 Active Issues

| ID    | Status     | Page / Component                                 | Description                                                                                                                                                                                                                                                                                                                                                                                                                        | Notes                                                                                                                                                                                                                                                           |
| ----- | ---------- | ------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C-001 | 🟢 `FIXED` | `BookingDetailDrawer.jsx` — Price section        | **"Booking Amount" shows `booking.price.total` (correct) but the refund display had a case-sensitivity bug.** Replaced title-case `"Refunded"` check with lowercase `"refunded"`.                                                                                                                                                                                                                                                  | **Fixed:** The refund section now renders correctly for all refunded/partially_refunded bookings.                                                                                                                                                               |
| C-002 | 🟢 `FIXED` | `BookingDetailDrawer.jsx` — Refund Display       | **Refund section lacked breakdown.** Customers only saw the original amount. Now shows "Refund Processed" and "Net Total: R 0".                                                                                                                                                                                                                                                                                                    | **Fixed:** UI now clearly displays the refund delta and the final net cost to the customer.                                                                                                                                                                     |
| C-003 | 🟢 `FIXED` | `BookingDetailDrawer.jsx` — `canDownloadInvoice` | **Invoice button was disabled until booking reached `completed` status.** Customers with a `confirmed` or `in-progress` (paid) booking couldn't download their invoice, and the tooltip message was confusing. | **Fixed:** `isInvoiceEnabled` now also returns `true` when `paymentStatus === "paid"`, or status is `"confirmed"` or `"in-progress"`. Tooltip updated to "Available after payment is confirmed". |
| C-004 | 🟢 `FIXED` | `useDashboardStore.js` — Seed/fallback data      | **`createBookingFromFlow` used hardcoded 5% platform fee** while production uses 10% from admin settings.                                                                                                                                                                                                                                                                                                                          | **Fixed (no action needed):** `createBookingFromFlow` is a dead legacy function — defined in the store but never called anywhere. All bookings go through the API (`POST /customer/bookings`). Fee is always determined server-side from admin settings. |
| C-005 | 🟢 `FIXED` | `BookingDetailDrawer.jsx` — Refund Banner        | **Customers confused by delay between "Refund Processed" in app and arrival of funds in bank.**                                                                                                                                                                                                                                                                                                                                    | **Fixed:** Paystack refund banner updated with correct link and 3-12 day timeline.                                                                                                                                                                              |
| C-006 | 🟢 `FIXED` | `BookingDetailDrawer.jsx` — Invoice Button       | **Invoice button visibility inconsistent.** Button visibility was not synced between the booking list and the detail drawer. Refund receipts were also not enabled for all bookings.                                                                                                                                                                                                                                               | **Fixed:** Invoice button visibility synced between list and drawer; refund receipts enabled for all.                                                                                                                                                           |
| C-007 | 🟢 `FIXED` | `PaymentModal.jsx` — Null selection crash        | **TypeError: Cannot read properties of null (reading 'amount')** when rendering. The surcharge calculation hook ran before checking if `payment` exists.                                                                                                                                                                                                                                                                           | **Fixed:** Added optional chaining to `payment?.amount` in the dependency array and effect body. Modal now handles null states gracefully.                                                                                                                      |
| N-001 | 🟢 `FIXED` | `BookingDetailDrawer.jsx` — Refund Receipt Date  | **Refund receipt date incorrect.** The refund receipt was showing the booking creation date instead of the actual refund processing date.                                                                                                                                                                                                                                                                                          | **Fixed:** Refund receipt date corrected to show refund processing date instead of creation date.                                                                                                                                                               |
| C-008 | 🟢 `FIXED` | `Quotes.jsx` — Deep Linking                      | **Quote invisible when arriving via deep link.** If a user clicked a quote from an email, and that quote was not "Open" (e.g., "Responses"), it was hidden by the default "Open" filter.                                                                                                                                                                                                                                           | **Fixed:** Updated `Quotes.jsx` to automatically switch the status filter to "All" if a specific `routeId` is provided and the found quote doesn't match the current filter.                                                                                    |
| C-009 | 🟢 `FIXED` | `BookSearch.jsx` — Service/Glass Type Select     | **ReferenceError: Check is not defined.** When a customer selects a service type or glass type, the app crashed because the `Check` icon from `lucide-react` wasn't imported, which was used recently for multi-select enhancements.                                                                                                                                                                                               | **Fixed:** Added the missing `Check` import from `lucide-react` in `BookSearch.jsx`.                                                                                                                                                                            |
| C-010 | 🟢 `FIXED` | `BookingForm` & `RequestQuoteModal`              | **Single-select only for Service Type and Glass Type.** Customer could not select multiple services or associated glass panes together.                                                                                                                                                                                                                                                                                            | **Fixed:** Refactored components to support `serviceTypes`, `glassTypes`, and a grouped `serviceSelections` array. Validation updated to require at least 1 selection.                                                                                          |
| C-011 | 🟢 `FIXED` | `Quotes` & `Booking` Details Display             | **Multi-select payload not rendering properly in lists/details.** The summary cards and detail panels only rendered string values.                                                                                                                                                                                                                                                                                                 | **Fixed:** Updated UI logic (`Quotes`, `QuoteDetail`, `BookingPending`, `BookingSearching`) to detect arrays and render properly joined string sequences. _Note: Robust fallbacks added so legacy string data safely arrays to `[string]` or renders natively._ |
| C-012 | 🟢 `FIXED` | `RequestQuoteModal.jsx` — Field Reordering       | **User flow optimization.** "Additional Notes" field was before "Upload Photos", which was non-intuitive for customers.                                                                                                                                                                                                                                                                                                            | **Fixed:** Relocated "Additional Notes" to the end of the form, after the image upload section.                                                                                                                                                                 |
| C-013 | 🟢 `FIXED` | `RequestQuoteModal.jsx` — City Coverage          | **Provider availability feedback.** Customers were requesting quotes in cities without active providers.                                                                                                                                                                                                                                                                                                                           | **Fixed:** Integrated dynamic backend coverage check. Cities without approved/active providers are now disabled and show "More providers coming soon". Added `isSearchable` mode to the City selector.                                                          |
| C-016 | 🟢 `FIXED` | `useDashboardStore.js` — Close Quote Tab Count   | **Tab counts not updating when a quote is closed.** Closing a quote set local state to `"Cancelled"` but the UI filter tabs check for `"Closed"`. `dataMappers.js` maps backend `cancelled` → `"Closed"`, but the optimistic update bypassed the mapper, causing the "Closed" tab count to stay at 0 until page refresh. | **Fixed:** Changed optimistic update in `closeQuoteRequest` from `status: "Cancelled"` → `status: "Closed"` to match the mapped value used by all tab filters. |

---

## 💡 Price Calculation Reference (Customer Portal)

What the customer sees in `BookingDetailDrawer.jsx`:

```
Booking Amount  = booking.price.total  (what they paid)
Refund Processed = booking.cancellation.refundAmount  (negative, shown in purple)
NET TOTAL       = booking.price.total - refundAmount  (= 0 for full refunds)
```

The customer portal never shows Platform Fee or Provider Earnings — those are internal.

> [!CAUTION]
> The `paymentStatus` comparison MUST be case-insensitive. The backend returns lowercase (`"refunded"`, `"paid"`, `"partially_refunded"`), but older check strings in the UI use title-case (`"Refunded"`, `"Paid"`). This causes silent display failures for refunded bookings.

| CLI-MULT | 🟢 `FIXED` | Multiple Portals — Service Arrays | **Multi-selection fields not resolving in lists/details** | **Fixed:** Updated dataMapping and lists across Provider Earnings/Jobs, Customer Payments, Admin Bookings/Quotes to iterate over serviceSelections arrays and render dynamically. |
| GLB-MEM | 🟢 `FIXED` | Backend Node — Sorting Limits | **Sort exceeded memory limit of 33554432 bytes** | **Fixed:** Addressed `allowDiskUse:true` to all pagination and document fetching Mongoose.find().sort() pipelines to stop out of memory errors. |
| CLI-SVCUI | 🟢 `FIXED` | Multiple Portals | **Service UI Refinement.** Tables showing service combinations were using a "View" button for everything. | **Fixed:** Removed the "View" button and modal entirely. Now renders services as a clean list directly in rows/cards (matching Payments style). |
| CLI-STAT | 🟢 `FIXED` | `StatusBadge.jsx` | **Status Color Discrepancy.** Some statuses (e.g. "Awaiting Provider") showed different colors on Dashboard vs Bookings pages. | **Fixed:** Updated `StatusBadge.jsx` config keys to include display labels (e.g., "Awaiting Provider", "Action Required") to ensure consistency when raw status is mapped. |
| C-014 | 🟢 `FIXED` | `DashboardSidebar.jsx` — Badge stale count from previous session | **Sidebar badge for "My Bookings" showed a count (e.g. 8) but Upcoming tab showed "No bookings yet".** Root cause: a previous session incorrectly added `awaiting-provider-acceptance` (with `slotNegotiation.status === "provider-proposed"`) to the badge count. These bookings are intentionally excluded from the Bookings page (they belong to the quote flow, tracked via Quotes page). Badge and page were out of sync. | **Fixed:** Removed `awaiting-provider-acceptance` from the sidebar badge condition. Badge now only counts `awaiting-customer-approval` and `searching` (with quotes/suggestions) — statuses that are visible and actionable on the Bookings page. `awaiting-provider-acceptance` is handled entirely in the Quotes page (QuoteDetailPanel). |
| C-015 | 🟢 `FIXED` | `QuoteDetailPanel.jsx` + `useDashboardStore.js` — Dual toast on accept | **Both "An error occurred" and "Quote accepted!" toasts appeared simultaneously** when accepting a quote. Two root causes: (1) `handleAcceptQuote` showed success toast even when `acceptQuote` returned null. (2) `acceptQuote` wrapped `fetchQuotes`/`fetchBookings` in the same try/catch — if the refresh failed after a successful acceptance, the catch block fired showing an error toast and returning null. | **Fixed:** (1) `handleAcceptQuote` now only shows success toast when `bookingId` is truthy. (2) `acceptQuote` captures the booking ID before refreshes, runs refreshes with `.catch(()=>{})` so their failures don't mask the successful acceptance. |
| GLB-AUTH | 🟢 `FIXED` | All Portals — Auth Initialization | **"Not authorized" 401 storm on page load.** Stale Zustand `isAuthenticated=true` caused protected pages to fire API calls without a valid token. | **No change needed:** Client portal `ProtectedRoute` already calls `initAuth()` and shows a loading spinner before rendering protected pages. The fix was applied to `autoscreen_admin` and `autoscreen_provider` portals only. |
| C-014 | 🟢 `FIXED` | `Quotes.jsx` / `useDashboardStore.js` — Quote 404 Loop | **Repeated 404 API calls for inaccessible quotes.** When a quote deep link doesn't belong to the logged-in user, `fetchQuoteDetails` fires repeatedly from page load, detail panel, and socket events. | **Fixed:** Added `_failedQuoteIds` tracking with 5-min cooldown in store. `Quotes.jsx` now shows error toast and redirects to `/dashboard/quotes` on 404. |
| C-015 | 🟢 `FIXED` | `SelectSlotModal.jsx` / `QuoteDetailPanel.jsx` — Empty Slots | **PremiumSelect showed no time slots.** `getQuotes` endpoint doesn't nest-populate `responses.provider`, so `provider` was a raw ObjectId string. `provider._id` on a string is undefined, preventing the availability API call. | **Fixed:** Added `typeof provider === "string"` check in `QuoteDetailPanel.jsx` to use the string directly as the provider ID. |
| C-016 | 🟢 `FIXED` | `useDashboardStore.js` / `QuoteDetailPanel.jsx` — Duplicate Toasts | **Two success toasts on quote acceptance.** Store's `acceptQuote` and component's `handleAcceptQuote` both fired success toasts. | **Fixed:** Removed the store-level toast; component provides more specific messaging. |
| C-017 | 🟢 `FIXED` | `RequestQuoteModal.jsx` — Schedule Fields | **Date/time fields still present in customer quote form.** Per the slot selection flow, scheduling should happen at quote acceptance time, not during quote submission. | **Fixed:** Removed Preferred Schedule section (PremiumDatePicker + time slot PremiumSelect). Payload sends `null` for both fields. |
| C-018 | 🟢 `FIXED` | `Bookings.jsx` — Awaiting Provider Visibility | **Bookings with "awaiting-provider-acceptance" status visible to customer before provider confirms time slot.** Customer should only see the booking once the time slot is confirmed. | **Fixed:** Filtered out `awaiting-provider-acceptance` status from the customer bookings list. Booking becomes visible after provider confirms (status changes to confirmed/accepted). |
| C-019 | 🟢 `FIXED` | `QuoteDetailPanel.jsx` / `QuoteDetail.jsx` — Quote Accept Redirect | **After accepting a quote, customer was redirected to `/dashboard/bookings/:id`.** The booking is in `awaiting-provider-acceptance` and filtered from the list, causing a blank page. | **Fixed:** Customer now stays on quotes page after acceptance. Shows success toast and refreshes quote details to display "Waiting for Provider Confirmation" banner. |
| C-020 | 🟢 `FIXED` | `DashboardTopBar.jsx` — Notification Routing | **"Quote Accepted" notification click routed to bookings page.** All quote/booking notifications were sent to `/dashboard/bookings/:id` regardless of type. For quote-based flow the booking was hidden. | **Fixed:** Notification click handler now routes `quote_accepted`, `slot_change_proposed`, `quote_response` types to `/dashboard/quotes/:quoteId`. Backend also updated to include `quoteId` in notification data. Direct booking notifications unchanged. |

| C-025 | 🟢 `FIXED` | `socketService.js` — Quote store not refreshed on `booking_confirmed` | **After a customer paid, `booking_confirmed` triggered `fetchBookings()` but not `fetchQuotes()`. The Quotes page / QuoteDetailPanel showed the old booking status (`awaiting-payment`) until a manual refresh, so the "Pay Now" banner persisted after payment was complete.** | **Fixed:** `booking_confirmed` and `booking_cancelled` now also call `fetchQuotes()` so QuoteDetailPanel reflects the updated booking status in real-time. |
| C-021 | 🟢 `FIXED` | `PaymentModal.jsx` — Hardcoded fee rate | **Fee calculation used hardcoded `0.029 * 1.15`.** Should use the backend-provided `variableFeeRate` from `getPaystackConfig`. | **Fixed:** Now fetches fee rate from backend on mount. Falls back to hardcoded value if fetch fails. |
| C-022 | 🟢 `FIXED` | `PaymentModal.jsx` — No URL validation | **No validation before `window.location.href = res.authorization_url`.** A compromised API response could redirect to a phishing page. | **Fixed:** Added `isPaystackUrl()` whitelist check before redirect. Only `checkout.paystack.com` and `standard.paystack.co` allowed. |
| C-023 | 🟢 `FIXED` | `PaymentModal.jsx` — Bare console.error | **`console.error("Payment Error:", error)` left in production code.** | **Fixed:** Wrapped in `import.meta.env.DEV` guard. |
| C-024 | 🟢 `FIXED` | `Quotes.jsx` / `dataMappers.js` — Unconfirmed quotes in wrong tab | **Quotes with `awaiting-provider-acceptance` booking status appeared in "Accepted" tab instead of "Responses" tab.** After customer accepts a quote, booking is created in `awaiting-provider-acceptance` status. The quote's status normalizes to "Accepted" but the time slot isn't confirmed yet. | **Fixed:** Added status override in `mapQuoteData`: if computed status is "Accepted" but `booking.status` is `awaiting-provider-acceptance`, override to "Responses". Backend `getQuotes` now populates `booking` with `status` and `slotNegotiation` fields. Quote moves to "Accepted" tab once provider confirms. |

| C-026 | 🟢 `FIXED` | `Bookings.jsx` + `bookingController.js` — My Bookings page shows "No bookings yet" | **All tab counts (Upcoming 0, Completed 0, Cancelled 0, All 0) showed zero even though completed/older bookings existed.** Root cause: The API default `limit=10` combined with `createdAt: -1` sort meant the first page was filled with recent `awaiting-payment` quote-flow bookings. The frontend then filtered those out, leaving an empty list. Older confirmed/completed bookings were never fetched (on page 2+). Secondary issue: `in-progress` bookings were missing from the Upcoming tab filter. | **Fixed (backend):** `getBookings` now excludes `awaiting-payment` and `awaiting-provider-acceptance` by default — these belong to the Quotes flow. **Fixed (frontend):** Added `"in-progress"` to Upcoming tab status list; removed the past-date exclusion (`scheduledDate > now`) from Upcoming count and filter (in-progress jobs can have past scheduled dates). |

| C-PROFILE | 🟢 `FIXED` | `Profile.jsx` — Default Location section | **Read-only "Default Location" row in profile settings was a non-editable duplicate** of the default saved address already visible in the Saved Addresses section above. User could not edit it. | **Fixed:** Removed the Default Location section entirely from the profile settings list. |

| C-027 | 🟢 `FIXED` | `utils/dataMappers.js` — Service ObjectId shown instead of name | **`formatServiceName()` fell through to render a raw 24-char hex ObjectId string** when `booking.service` was an unpopulated ObjectId reference. Quote-based bookings often have `service` as a bare ObjectId (not populated), so service names showed as e.g. `"67a1e4035998a600683de67c"` on Bookings list and detail drawer. | **Fixed:** Added `/^[a-f\d]{24}$/i` regex guard — if `booking.service` is an ObjectId string it is skipped and fallback to `serviceType` is used instead. |
| C-028 | 🟢 `FIXED` | `pages/dashboard/Bookings.jsx` — Cancel missing reason | **`bookingService.cancelBooking(cancelBooking.id)` called without a `reason` argument.** The service accepts `(bookingId, reason)` and sends it as `data.reason`. Without it, the backend defaulted to undefined and logged no reason. | **Fixed:** Updated call to `bookingService.cancelBooking(cancelBooking.id, "Customer requested cancellation")`. |
| C-029 | 🟢 `FIXED` | `App.jsx` — Unused old booking-flow imports and routes | **7 old direct-booking pages (BookSearch, ProviderList, ProviderProfile, BookingForm, BookingSearching, BookingPending, BookingConfirmation) were still imported and routed** even though the platform is quote-only and these pages were unreachable. Added bundle weight and import confusion. | **Fixed:** Removed all 7 imports, their 5 routes (`/book`, `/book/request`, `/booking/searching/:id`, `/booking/pending/:id`, `/booking/confirmation/:id`), and 2 commented-out old imports. Page files also deleted from disk. |

| C-030 | 🟢 `FIXED` | `pages/dashboard/BookAppointment.jsx` — Reschedule page crash "Objects are not valid as a React child" | **Reschedule page crashed because raw booking API data uses `vehicle.make`/`serviceAddress` subdocuments, but the code accessed flat mapped names (`vehicleMake`, `location`) that don't exist on raw data.** `vehicleFormatted` resolved to empty string, fallback `quote.vehicle` was an object `{ registrationNumber }`, rendered directly as React child → crash. Location also empty. | **Fixed:** (1) Changed `b.vehicleMake`→`b.vehicle?.make`, `b.vehicleModel`→`b.vehicle?.model`, `b.vehicleYear`→`b.vehicle?.year`, `b.vehicleRegNumber`→`b.vehicle?.registrationNumber`. (2) Changed `b.location`→`b.serviceAddress \|\| b.location`. (3) Added `typeof` guard on `vehicleStr` fallback to prevent object rendering. |

| C-031 | 🟢 `FIXED` | Full portal — Mobile responsiveness | **Multiple mobile responsiveness issues across all dashboard pages and shared components.** Modals exceeded 320px viewport, buttons didn't stack, grids overflowed, padding too spacious, Overview table had no mobile fallback, Messages used `100vh` ignoring mobile browser chrome. | **Fixed:** 15 files updated with responsive Tailwind classes. Card/modal/drawer padding responsive, buttons stack on mobile, image grids use fewer columns, tabs horizontally scrollable, Overview table replaced with mobile card list, Messages uses `100dvh`. See CLAUDE.md changelog 20 March 2026. |

| C-032 | 🟢 `FIXED` | `BookingCard.jsx` — Footer buttons overflow on mobile | **With 3 action buttons (workshop bookings: Reschedule + Directions + Cancel), the horizontal button row overflows the 320px viewport.** Footer layout was `flex items-center justify-between` with all buttons in a non-wrapping row. | **Fixed:** Footer restructured to `flex-col sm:flex-row`. Buttons stack vertically full-width on mobile, row on sm+. All buttons get `justify-center` and `w-full sm:w-auto`. |
| C-033 | 🟢 `FIXED` | `SupportChatPopup.jsx` — Chat icon overlays modals | **Chat popup used `z-50`, same as Modal/Drawer.** Because it's rendered later in the DOM, it visually sat on top of open modals. | **Fixed:** Lowered chat popup to `z-40`. Raised Toast to `z-[60]`. Z-index hierarchy: chat (z-40) < modal/drawer (z-50) < toast (z-60). |

| C-034 | 🟢 `FIXED` | `DashboardTopBar.jsx` — Theme toggle tooltip hidden off-screen | **The Tooltip wrapping the theme toggle used default `position="top"`, rendering the tooltip above the button. Since the header is fixed at the very top of the viewport, the tooltip was completely hidden off-screen.** | **Fixed:** Header fully redesigned to match provider portal. Custom `HeaderTooltip` component (CSS group-hover, bottom position) replaces both the old `Tooltip` component and native `title` attributes across all header buttons. |
| C-035 | 🟢 `FIXED` | `DashboardTopBar.jsx` + `DashboardSidebar.jsx` — Header design mismatch with provider portal | **Customer portal header used an old flat design (h-16, white bg, no breadcrumbs, old-style dropdowns) while provider portal had a premium redesigned header (glass-bg, 68px, breadcrumbs, 36px round buttons, pill account button, premium dropdowns).** Sidebar collapse icon also used `ChevronLeft`/`ChevronRight` instead of `PanelLeftClose`/`PanelLeftOpen`. | **Fixed:** (1) Header fully rewritten to match provider design: sticky glass-bg, 68px height, breadcrumb nav on desktop, 36px round action buttons, pill account button with gradient avatar, premium notification/account dropdowns. (2) New `Breadcrumb.jsx` created with customer routes. (3) Sidebar collapse icons changed to `PanelLeftClose`/`PanelLeftOpen`. (4) CSS layout tokens (`--topbar-h`, `--sidebar-w`, `--sidebar-collapsed-w`, `glass-bg`) added to `index.css`. (5) DashboardLayout updated with CSS variable-based sidebar offset and matching padding. |

| C-036 | 🟢 `FIXED` | `NewQuote.jsx` + `RequestQuoteModal.jsx` — Quote image upload "request too large" error | **Images were sent as base64 data URLs in the JSON body, causing Nginx to return 413 (payload too large) even for small images.** | **Fixed:** `ImageUploadSlot` now stores original `File` alongside base64 preview. `quoteService.uploadImages()` uploads files via FormData to `POST /api/public/upload-quote-images`. Submission sends file URLs instead of base64. |

_Portal: **autoscreen_client** | Last updated: 23 March 2026_
